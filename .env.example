# =============================================================================
# API Configuration
# =============================================================================

# Main API Base URL - used for all backend API calls
# In production, this should be your deployed backend URL
# In development, this points to your local backend server
PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Custom API Configuration for chatbot builder
# This is specifically for the chatbot builder functionality
PUBLIC_CUSTOM_API_BASE_URL=http://127.0.0.1:8000

# Frontend-only mode configuration
# Set to 'true' when using only the chatbot builder without a full backend server
# Set to 'false' when you have a full Swift backend running
PUBLIC_FRONTEND_ONLY=false

# =============================================================================
# Legacy Environment Variables (for backward compatibility)
# =============================================================================

# Languages API Configuration (legacy)
LANGUAGES_API_BASE_URL=http://127.0.0.1:8000

# Vite API Base URL (legacy)
VITE_API_BASE_URL=http://127.0.0.1:8000/

# =============================================================================
# External Service Configuration
# =============================================================================

# Ollama URL for the backend to connect
# The path '/ollama' will be redirected to the specified backend URL
OLLAMA_BASE_URL=http://localhost:11434

# OpenAI API Configuration
OPENAI_API_BASE_URL=
OPENAI_API_KEY=

# AUTOMATIC1111_BASE_URL="http://localhost:7860"

# For production, you should only need one host as
# fastapi serves the svelte-kit built frontend and backend from the same host and port.
# To test with CORS locally, you can set something like
# CORS_ALLOW_ORIGIN='http://localhost:5173;http://localhost:8080'
CORS_ALLOW_ORIGIN='*'

# For production you should set this to match the proxy configuration (127.0.0.1)
FORWARDED_ALLOW_IPS='*'

# DO NOT TRACK
SCARF_NO_ANALYTICS=true
DO_NOT_TRACK=true
ANONYMIZED_TELEMETRY=false